import { MainDirectiveDirective } from './main-directive.directive';

describe('MainDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MainDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
